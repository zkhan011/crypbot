# Tenant administrator guide

Tenant administration is a production follow-up gated by the durable schema migration. Tenant administrators must be constrained to their tenant’s users, bots, reports, AI drafts, credentials, and audit events; Super Admin access is the only cross-tenant role.

Until tenant-scoped repositories and endpoints are enabled, do not onboard customer tenants or credentials. Use the single local MOCK tenant only for demonstrations.
